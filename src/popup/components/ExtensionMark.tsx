import iconUrl from "../assets/icon-48.png?url";

/**
 * Vite-resolved manifest icon URL (`assets/icon-48.png`). Imported by `PopupHeader` only;
 * not a separate header React component (shared chrome lives in `@helvety/extension-chrome`).
 */
export const EXTENSION_ICON_URL = iconUrl;
